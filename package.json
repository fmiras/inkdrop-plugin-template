{
  "name": "template",
  "main": "./lib/index",
  "version": "0.0.0",
  "description": "Use this easy template to create Inkdrop Plugins!",
  "repository": "https://github.com/fmiras/inkdrop-plugin-renderer-template",
  "license": "MIT",
  "scripts": {
    "build": "swc ./src -d lib",
    "watch": "swc ./src -d lib -w"
  },
  "engines": {
    "inkdrop": "^5.3.1"
  },
  "dependencies": {
    "react-resize-detector": "^8.0.3"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.61",
    "@swc/core": "^1.3.32",
    "chokidar": "^3.5.3"
  }
}
